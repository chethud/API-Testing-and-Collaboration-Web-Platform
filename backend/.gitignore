# Python
venv/
__pycache__/
*.py[cod]
.env
.backend-port

# Leftover Node (remove when node_modules is deleted manually)
node_modules/

# SQLite (optional: ignore DB so each dev has local)
# *.sqlite
